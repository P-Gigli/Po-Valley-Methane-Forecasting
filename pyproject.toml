[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "po-valley-methane-forecasting"
version = "0.1.0"
description = "Weekly XCH4 forecasting over the Po Valley"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]